<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ replacement for the allowBackup flag. Nothing is eligible for
  cloud backup or device-to-device transfer: the WebView data directory holds
  the live Supabase session, and a copy of it on another device is a copy of the
  user's login. Signing in again on a new device costs one OTP.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
