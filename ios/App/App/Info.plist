<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Wedding Mall</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!--
	  The app talks to Supabase over HTTPS and ships no encryption of its own,
	  so it qualifies for the exemption. Declaring it here stops App Store
	  Connect asking the export-compliance question on every single upload.
	-->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!--
	  The biodata maker's photo picker is the only place the app reads an image,
	  and that image is rendered into the PDF on the device and never uploaded.
	  Apple rejects vague purpose strings, so both say exactly that.
	-->
	<key>NSCameraUsageDescription</key>
	<string>Wedding Mall uses the camera only when you choose to take a photo for your matrimonial biodata. The photo stays on this device.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Wedding Mall reads a photo from your library only when you pick one for your matrimonial biodata. The photo stays on this device.</string>
	<!--
	  Deep links. This custom scheme (online.weddingmall.app://blogs/<slug>)
	  needs no server-side setup. Universal Links over https additionally
	  require the Associated Domains capability in Xcode and an
	  apple-app-site-association file on weddingmall.online — see
	  docs/DEPLOYMENT.md. AppDelegate already forwards both to Capacitor.
	-->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>online.weddingmall.app</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>online.weddingmall.app</string>
			</array>
		</dict>
	</array>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- iOS 15 is the deployment target, so every supported device is arm64. -->
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
</dict>
</plist>
